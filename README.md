# Ravuthz's dotfiles

```bash

git clone https://github.com/ravuthz/dotfiles ~/dotfiles


cd -C ~/dotfiles

make help
make backup
make restore

# Or

make -C ~/dotfiles help
make -C ~/dotfiles backup
make -C ~/dotfiles restore



https://wezterm.org/colorschemes/a/index.html#andromeda
code ~/.config/wezterm/wezterm.lua

```
